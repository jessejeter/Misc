#include <stdio.h>

// This is a comment line. In modern C, every function has an implicit return type. 
// In the case of main() that return type is an integer. If the program gets to the 
// last line we return 0 by convention to signal all went well 
//
int main() {

  printf("Hello World.");
	
  return 0;
}
